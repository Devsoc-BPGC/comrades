<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="coordinatorLayoutStyle">@style/Widget.Support.CoordinatorLayout</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ToolBarStyle" parent="AppTheme">
        <item name="android:textColorPrimary">@color/colorPrimaryContrast</item>
        <item name="android:textColorSecondary">@color/colorPrimaryContrast</item>
        <item name="actionMenuTextColor">@color/colorPrimaryContrast</item>
    </style>

    <style name="AppTheme.PopupTheme" parent="AppTheme">
        <item name="android:textColorPrimary">@color/colorPrimaryContrast</item>
        <item name="android:textColorSecondary">@color/colorPrimaryContrast</item>
        <item name="actionMenuTextColor">@color/colorPrimaryContrast</item>
    </style>

    <style name="animation">
        <item name="android:windowEnterAnimation">@animator/popup_show</item>
        <item name="android:windowExitAnimation">@animator/popup_hide</item>
    </style>

</resources>
